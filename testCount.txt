 netstat -ano | Select-String "LISTENING" | Select-String "3001|5173"

(Invoke-RestMethod "http://localhost:3001/api/records").data.Count 

Get-Process -Id 12345

netstat -ano | findstr ":3001 :5173"

taskkill /PID <那个PID> /F

npx playwright test
npx playwright show-report

重启前端
cd c:/Users/User/Desktop/stacy/testCount/client && npx vite --host 2>&1

http://localhost:3001/api/health 有返回 → 后端活着

http://localhost:5173 打不开 → 前端挂了

npm run dev
等价于 npx tsx watch src/index.ts，直接跑 .ts 源码，改了代码还自动重启。

npm start，
node dist/index.js 跑编译后的 JS（生产用），要先 npm run build（tsc 编译）才有 dist/


$body = @{type='expense'; amount=555; category='餐饮'; date='2026-07-13'; note='午饭'} | ConvertTo-Json -Compress
[System.IO.File]::WriteAllText("req.json", $body, [System.Text.Encoding]::UTF8)
Invoke-RestMethod -Uri "http://localhost:3001/api/records/1" -Method PUT -ContentType "application/json" -InFile "req.json"


data-testid 会跟着打包进生产环境，什么是生产环境，怎么打包？
